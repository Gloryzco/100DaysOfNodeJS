const add = (one, two) => {
    const result = one + two;
    console.log(result);
}

const subtract = (one, two) => {
    const result = one - two;
    console.log(result);
}

module.exports = {add, subtract}